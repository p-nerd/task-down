import { AppLayout } from "@/components/layouts/app-layout";

const Pomodoro = () => {
    return (
        <AppLayout>
            <h1>Pomodoro</h1>
        </AppLayout>
    );
};

export default Pomodoro;
